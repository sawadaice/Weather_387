import { defineStore } from 'pinia';

export const useWeatherStore = defineStore({
  id: 'weather',
  state: () => ({
    weatherData: [],
    NewWTdata: [],
  }),
  getters: {   
    
  },
  actions: {
    
  },
});
